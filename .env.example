# Google OAuth2 Configuration
# 
# To obtain these credentials:
# 1. Go to https://console.cloud.google.com/
# 2. Create a new project or select an existing one
# 3. Enable Google Gmail API and Google Sheets API
# 4. Go to "APIs & Services" > "Credentials"
# 5. Create OAuth 2.0 Client ID for "Desktop app"
# 6. Add authorized redirect URIs (see below)
# 7. Copy the Client ID and Client Secret here
#
# IMPORTANT: Never commit this file with real credentials!
# The .env file is gitignored for security.

# Google OAuth2 Client ID
# Format: xxxxxx.apps.googleusercontent.com
GOOGLE_OAUTH_CLIENT_ID=your_client_id_here.apps.googleusercontent.com

# Google OAuth2 Client Secret
# Format: GOCSPX-xxxxxxxxxxxxx
GOOGLE_OAUTH_CLIENT_SECRET=your_client_secret_here

# Redirect URI for OAuth callbacks
# For macOS: com.yourcompany.decisionagent:/oauth/callback
# Must match CFBundleURLSchemes in macos/Runner/Info.plist
# For other platforms, use appropriate URL scheme
GOOGLE_OAUTH_REDIRECT_URI=com.yourcompany.decisionagent:/oauth/callback

# Optional: OpenAI API Key
# Only required if you want to use AI features
# Get your key from: https://platform.openai.com/api-keys
# You can also set this in the app Settings after installation
# OPENAI_API_KEY=sk-your_key_here
